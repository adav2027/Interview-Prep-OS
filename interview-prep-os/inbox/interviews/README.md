# inbox/interviews/

Drop your interview inputs here, one folder per company:

```
inbox/interviews/<company-slug>/
├── job-description.md      # paste the JD
└── interviewers.md         # names, titles, and the schedule
```

Then ask your agent: **"Run /prep-interview for `<company>`"**. Generated prep docs land in `outputs/<company-slug>/`.
